version: "3.9"

services:
  proxy:
    build: ./proxy
    image: dev-env-proxy:latest
    ports:
      - "80:80"
    depends_on:
      - frontend
      - backend
      - swagger
      - adminer
    networks:
      - app-net

  swagger:
    image: swaggerapi/swagger-ui:latest
    environment:
      BASE_URL: /swagger/
      SPEC_URL: /api-docs.json
    networks:
      - app-net

  adminer:
    image: adminer:latest
    environment:
      ADMINER_DEFAULT_SERVER: db
      ADMINER_DESIGN: pepa-linha
    networks:
      - app-net

  frontend:
    build: ./frontend
    image: dev-env-frontend:latest
    networks:
      - app-net

  backend:
    build: ./backend
    image: dev-env-backend:latest
    environment:
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432
      POSTGRES_USER: devuser
      POSTGRES_PASSWORD: devpass
      POSTGRES_DB: devdb
      REDIS_URL: redis://redis:6379
      NODE_ENV: development
    depends_on:
      - db
      - redis
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - app-net

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: devuser
      POSTGRES_PASSWORD: devpass
      POSTGRES_DB: devdb
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      - app-net

  redis:
    image: redis:7-alpine
    networks:
      - app-net

networks:
  app-net:
    driver: bridge

volumes:
  db-data:

